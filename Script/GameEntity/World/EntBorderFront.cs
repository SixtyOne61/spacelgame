using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EntBorderFront : EntBorder
{
    /*
    protected override void GenerateBorderPoint()
    {
        int sizeX = (int)ParamNbChunck.Value * ParamMap.Width;
        int sizeY = (int)ParamNbChunck.Value * ParamMap.Height;
        for(int y = 0; y <= sizeY; y += ParamMap.Height)
        {
            for (int x = 0; x < sizeX; ++x)
            {
                CompMeshGenerator.PosList.Add(new UnitPos(x, y, -1));
            }
        }

        for(int x = 0; x <= sizeX; x += ParamMap.Width)
        {
            for(int y = 0; y < sizeY; ++y)
            {
                CompMeshGenerator.PosList.Add(new UnitPos(x, y, -1));
            }
        }
    }
    */
}
